<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (error) { %>
      <p>Error getting dynamic content</p>
    <% } else {%>
      <p>Items:</p>
      <ul>
        <li>Number of categories: <%= data.categoryCount %></li>
        <li>Number of items: <%= data.itemCount %></li>
      </ul>
    <% } %>

  </body>
</html>
